services:
  netbox:
    ports:
      - "8050:8080"
    env_file: stack.env
    restart: unless-stopped
  netbox-worker:
    restart: unless-stopped
  netbox-housekeeping:
    restart: unless-stopped

  postgres:
    env_file: stack.env
    restart: unless-stopped

  redis:
    env_file: stack.env
    restart: unless-stopped

  redis-cache:
    env_file: stack.env
    restart: unless-stopped
